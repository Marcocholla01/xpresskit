
# Save exact versions in package.json (no ^ or ~)
save-exact=true

# Set default registry
registry=https://registry.npmjs.org/

# Prevent accidental global installs
package-lock=false

# Prevent package-lock.json from being generated
# (Bun and pnpm use their own lock files)
